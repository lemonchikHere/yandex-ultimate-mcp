# Roadmap

- [ ] Add optional `aikts/yandex-tracker-mcp` backend via `uvx` for expanded Tracker coverage.
- [ ] Add module-specific health probes that validate credentials with read-only API calls.
- [ ] Add generated tool catalog snapshots in CI for reproducible README counts.
- [ ] Add config preset generator for Codex, Claude Desktop, Cursor and VS Code.
- [ ] Add Docker image with Node + uv for all optional modules.
- [ ] Add typed wrappers for high-value read-only workflows: SEO audit, ads health, Metrika report bundle.
